# Day 1 任务清单

## 今日目标

完成茶饮咖啡竞品分析系统的项目初始化，让后续开发可以按后端、前端、数据、AI 和文档分工推进。

## 已完成

- 重写项目 README，明确项目目标、核心功能和技术栈。
- 建立后端目录 `backend/`，加入 FastAPI 入口文件和配置模块。
- 建立前端目录 `frontend/`，确定 Vue 3 + Vite + Element Plus + ECharts 技术路线。
- 建立样例数据目录 `data/sample/`，加入 12 个茶饮咖啡品牌基础数据。
- 建立项目文档目录 `docs/`，加入数据字典和两周开发计划。
- 更新 `.env.example`，补充 DeepSeek、数据库、前后端地址等配置项。
- 更新 `.gitignore`，避免提交虚拟环境、依赖目录、生成文件和旧练习文件。

## 明日任务

- 安装后端依赖。
- 启动 FastAPI。
- 验证 `/health` 接口。
- 设计数据库连接与品牌表结构。
- 准备第一次 Git 提交和分支整理。
